# SIL backend

Placeholder for a future POSIX/Linux software-in-the-loop implementation of
the IO layer. A SIL build mirrors `io/h747/` with simulated SPI, UART, EXTI,
and DShot impls; the `dev/` and `app/` layers above link unchanged.
