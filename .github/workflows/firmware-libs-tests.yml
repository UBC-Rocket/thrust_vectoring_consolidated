name: Firmware Libraries Tests

on:
  workflow_dispatch:
  push:
    paths:
      - 'embedded-software/firmware/lib/state_estimation/**'
      - 'embedded-software/firmware/lib/controls/**'
      - 'embedded-software/firmware/lib/messages/**'
      - 'embedded-software/firmware/lib/unity/**'
  pull_request:
    paths:
      - 'embedded-software/firmware/lib/state_estimation/**'
      - 'embedded-software/firmware/lib/controls/**'
      - 'embedded-software/firmware/lib/messages/**'
      - 'embedded-software/firmware/lib/unity/**'

jobs:
  test-state-estimation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        working-directory: embedded-software/firmware/lib/state_estimation/tests
        run: cmake -B build

      - name: Build
        working-directory: embedded-software/firmware/lib/state_estimation/tests
        run: cmake --build build

      - name: Run tests
        working-directory: embedded-software/firmware/lib/state_estimation/tests
        run: ctest --test-dir build --output-on-failure

  test-controls:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        working-directory: embedded-software/firmware/lib/controls/tests
        run: cmake -B build

      - name: Build
        working-directory: embedded-software/firmware/lib/controls/tests
        run: cmake --build build

      - name: Run tests
        working-directory: embedded-software/firmware/lib/controls/tests
        run: ctest --test-dir build --output-on-failure

  test-messages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        working-directory: embedded-software/firmware/lib/messages/tests
        run: cmake -B build

      - name: Build
        working-directory: embedded-software/firmware/lib/messages/tests
        run: cmake --build build

      - name: Run tests
        working-directory: embedded-software/firmware/lib/messages/tests
        run: ctest --test-dir build --output-on-failure
