# Controls Prototyping

MATLAB/Simulink models and analysis scripts for control system design.

The production C implementations live in `firmware/libs/controls/` (PID controller, flight controller with torque control, control allocation, and gimbal angle computation).
